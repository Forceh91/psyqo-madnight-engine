name: Tests

on:
  push:
    branches: [main, refactor/better-pool-management]
  pull_request:
    branches: [main, refactor/better-pool-management]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pool-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run tests
        run: make -C tests test
